<% @release.images.each do |image| %>
  #<%= "img-" + image.id.to_s %> {
    @include image-set('<%= image.thumb.url %>', '<%= image.thumb_high.url %>') {
      width: <%= image.thumb.width %>px;
      height: <%= image.thumb.height %>px;
    }
  }
<% end %>