<!DOCTYPE html>
<html>
  <head>
    <title>Music</title>
    <meta name="viewport" content="initial-scale=1, maximum-scale=1" />
    <%= stylesheet_link_tag :application, media: :all %>
    <%= javascript_include_tag :application %>
    <%= csrf_meta_tags %>
    <%= yield(:head) if defined?(:head) %>
    <%= favicon_link_tag 'favicon.ico' %>
  </head>
  <body>
    <%= yield %>
  </body>
</html>